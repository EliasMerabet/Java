package webservices2.cliente;

import java.time.Clock;

/**
 *
 * @author alumno
 */
public class WebServices2Cliente {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        System.out.println(nombre());
    }

    private static String nombre() {
        ClienteWS.Proveedor_Service service = new ClienteWS.Proveedor_Service();
        ClienteWS.Proveedor port = service.getProveedorPort();
        return port.nombre();
    }
    
    
    
}
